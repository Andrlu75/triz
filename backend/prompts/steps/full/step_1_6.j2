{# step_1_6.j2 -- Шаг 1.6 полного АРИЗ-2010: Модель задачи #}
{# Часть 1: Анализ задачи (правила 15-16) #}
{# Переменные: user_input, problem_title, problem_description, previous_steps, previous_results #}

## Часть 1: Анализ задачи -- Шаг 1.6 из {{ total_steps | default(24) }}: Модель задачи

> **Правила 15-16 АРИЗ-2010 (В. Петров). Формулировка модели задачи -- итог Части 1.**

### Контекст задачи:
**Задача:** {{ problem_title | default("") }}

{% if previous_results %}
**Мини-задача:** {{ previous_results.mini_task | default("") }}
**Конфликтующая пара:** {{ previous_results.conflicting_pair | default("") }}
**Инструмент:** {{ previous_results.instrument | default("") }}
**Объект/изделие:** {{ previous_results.product | default("") }}
**Выбранная схема ТП:** {{ previous_results.selected_tp | default("") }}
**Усиленный конфликт:** {{ previous_results.amplified_conflict | default("") }}
**ГФ:** {{ previous_results.main_function | default("") }}
{% endif %}

{% if previous_steps %}
### Предыдущие результаты:
{% for step in previous_steps %}
- **Шаг {{ step.step_code }}:** {{ step.validated_result | default(step.llm_output) }}
{% endfor %}
{% endif %}

{% if user_input %}
### Ввод пользователя:
{{ user_input }}
{% endif %}

---

### Задача шага 1.6: Записать формулировку модели задачи

Модель задачи -- итоговая формулировка результатов анализа из шагов 1.1-1.5. Она объединяет конфликтующую пару, усиленный конфликт и требования к X-элементу в единую структурированную формулу.

**1. Структура модели задачи (правило 15):**

Модель задачи включает:
- **Конфликтующая пара:** [инструмент] и [объект] -- конкретные элементы.
- **Усиленная формулировка конфликта:** [предельное состояние параметра] + [максимум вреда].
- **Требование к X-элементу:** что должен сделать X-элемент, чтобы разрешить конфликт.

Шаблон:
> **Модель задачи:**
> Дано: [инструмент] и [объект].
> При [предельном состоянии конфликта] [вредное действие максимально].
> Необходимо найти X-элемент, который, не усложняя систему и не вызывая новых вредных эффектов, устраняет [вредное действие], сохраняя способность [инструмента] выполнять [полезное действие] по отношению к [объекту].

**2. Предварительная формулировка ИКР-1 (правило 16):**

На основе модели задачи запишите ИКР-1 (первоначальный идеальный конечный результат):

> **ИКР-1:** X-элемент, абсолютно не усложняя систему и не вызывая вредных эффектов, устраняет [вредное действие] в течение оперативного времени в пределах оперативной зоны, сохраняя способность [инструмента] выполнять [полезное действие] по отношению к [объекту].

Примечание: Оперативная зона (ОЗ) и оперативное время (ОВ) будут определены в Части 2.

**3. Проверка модели задачи:**

- Указана ли конкретная конфликтующая пара (не абстрактные понятия)?
- Конфликт усилен до предела (нет компромисса)?
- X-элемент не предопределяет конкретное решение?
- Формулировка содержит и полезное, и вредное действие?
- Нет специальных терминов (правило 15)?

---

### Формат ответа:

```
МОДЕЛЬ ЗАДАЧИ (правило 15):
  Дано: [инструмент] и [объект].
  При [предельное состояние конфликта] [вредное максимально].
  Необходимо найти X-элемент, который, не усложняя систему
  и не вызывая новых вредных эффектов, устраняет [вредное],
  сохраняя [полезное] [инструмента] по отношению к [объекту].

ИКР-1 (правило 16):
  X-элемент, абсолютно не усложняя систему и не вызывая
  вредных эффектов, устраняет [вредное] в течение ОВ
  в пределах ОЗ, сохраняя способность [инструмента]
  выполнять [полезное] по отношению к [объекту].

ПРОВЕРКА МОДЕЛИ:
  Конкретная пара: ДА/НЕТ
  Предельное усиление: ДА/НЕТ
  X-элемент абстрактен: ДА/НЕТ
  Полезное + вредное: ДА/НЕТ
  Нет спецтерминов: ДА/НЕТ
```
