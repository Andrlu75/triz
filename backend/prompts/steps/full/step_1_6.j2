{# step_1_6.j2 — Full ARIZ Part 1, Step 1.6: Task model formulation #}
{# Variables: user_input, problem_title, problem_description, previous_steps, previous_results #}

## Часть 1 — Анализ задачи. Шаг 1.6: Модель задачи

### Контекст:
**Задача:** {{ problem_title | default("") }}

{% if previous_results %}
**Мини-задача:** {{ previous_results.mini_task | default("") }}
**Конфликтующая пара:** {{ previous_results.conflicting_pair | default("") }}
**Усиленный конфликт:** {{ previous_results.amplified_conflict | default("") }}
{% endif %}

{% if previous_steps %}
### Предыдущие результаты:
{% for step in previous_steps %}
- **Шаг {{ step.step_code }}:** {{ step.validated_result | default(step.llm_output) }}
{% endfor %}
{% endif %}

### Ввод пользователя:
{{ user_input | default("") }}

---

### Инструкция (правило 13):

**Шаг 1.6 АРИЗ-2010** — записать модель задачи: конфликтующая пара + усиленная формулировка конфликта.

**1. Записать модель задачи:**
> **Конфликтующая пара:** [инструмент] и [объект].
> **Усиленная формулировка:** [предельное состояние конфликта].
> **X-элемент:** [что должен обеспечить идеальный X-элемент].

**2. Записать формулировку ИКР-1 (правило 13):**
> «X-элемент, абсолютно не усложняя систему и не вызывая вредных эффектов, устраняет [вредное действие] в течение [ОВ] в пределах [ОЗ], сохраняя способность [инструмента] выполнять [полезное действие] по отношению к [объекту].»

**3. Проверить модель:**
- Указана ли конкретная конфликтующая пара?
- Усилен ли конфликт до предела?
- X-элемент не предопределяет конкретное решение?

### Формат ответа:
```
МОДЕЛЬ ЗАДАЧИ:
  Конфликтующая пара: [инструмент] + [объект]
  Усиленная формулировка: ...
  X-элемент: ...

ИКР-1: X-элемент, абсолютно не усложняя систему и не вызывая вредных эффектов, ...

ПРОВЕРКА:
  Конкретная пара: ДА/НЕТ
  Предельное усиление: ДА/НЕТ
  X-элемент не предопределяет решение: ДА/НЕТ
```
