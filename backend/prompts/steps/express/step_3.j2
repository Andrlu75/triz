## Шаг 3: Углублённое противоречие (УП)

### Контекст:
Задача: {{ context.get("task_formulation", "") }}
ПП: {{ context.get("surface_contradiction", "") }}

Ввод пользователя:
{{ user_input }}

{% if context.get("previous_steps") %}
### Предыдущие шаги:
{% for step in context.previous_steps %}
- Шаг {{ step.code }}: {{ step.result }}
{% endfor %}
{% endif %}

### Твоя задача:
1. Углуби поверхностное противоречие в углублённое (УП).
2. Определи конфликтующую пару: инструмент и объект.
3. Сформулируй УП по шаблону:
   - «Если [инструмент обладает свойством A], то [достигается полезный эффект], НО [возникает вредный эффект]»
   - «Если [инструмент обладает свойством B], то [устраняется вредный эффект], НО [теряется полезный эффект]»
4. Запиши оба варианта УП (правило 19).
5. Укажи, какое свойство вступает в конфликт.
