## Шаг 5: Обострённое противоречие (ОП)

### Контекст:
Задача: {{ context.get("task_formulation", "") }}
УП: {{ context.get("deepened_contradiction", "") }}
ИКР: {{ context.get("ikr", "") }}

Ввод пользователя:
{{ user_input }}

{% if context.get("previous_steps") %}
### Предыдущие шаги:
{% for step in context.previous_steps %}
- Шаг {{ step.code }}: {{ step.result }}
{% endfor %}
{% endif %}

### Твоя задача:
1. Сформулируй обострённое (физическое) противоречие (ОП):
   - «[Элемент] должен обладать свойством [S], чтобы [выполнять полезную функцию], И должен обладать свойством [анти-S], чтобы [удовлетворять ИКР / устранить вредный эффект]».
2. ОП — это предельное обострение УП до физического противоречия.
3. Одному и тому же элементу предъявляются два взаимоисключающих требования.
4. Усиль ОП: замени «должен» на «обязан», подчеркни абсолютный характер требований (правила 22-24).
