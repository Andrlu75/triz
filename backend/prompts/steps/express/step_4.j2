## Шаг 4: Идеальный конечный результат (ИКР)

### Контекст:
Задача: {{ context.get("task_formulation", "") }}
ПП: {{ context.get("surface_contradiction", "") }}
УП: {{ context.get("deepened_contradiction", "") }}

Ввод пользователя:
{{ user_input }}

{% if context.get("previous_steps") %}
### Предыдущие шаги:
{% for step in context.previous_steps %}
- Шаг {{ step.code }}: {{ step.result }}
{% endfor %}
{% endif %}

### Твоя задача:
1. Сформулируй идеальный конечный результат (ИКР):
   - «[Элемент системы] САМ [выполняет нужное действие], не вызывая [вредных эффектов] и не усложняя систему».
2. ИКР должен быть максимально «идеальным» — без затрат ресурсов, без усложнения.
3. Определи зону конфликта — где именно в системе возникает противоречие.
4. Определи оперативное время — когда возникает конфликт.
5. Сформулируй усиленный ИКР с учётом зоны и времени конфликта.
